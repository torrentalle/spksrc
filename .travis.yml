sudo: required

language: false

services:
  - docker

env:
  - SYNOARCH=88f6281
#  - SYNOARCH=alpine
#  - SYNOARCH=armada370
#  - SYNOARCH=armada375
#  - SYNOARCH=armada38x
#  - SYNOARCH=armadaxp
#  - SYNOARCH=comcerto2k
#  - SYNOARCH=evansport
#  - SYNOARCH=monaco
#  - SYNOARCH=ppc853x
#  - SYNOARCH=qoriq
#  - SYNOARCH=x64

before_install:
  - docker pull synocommunity/spksrc

script:
  - docker run -it -v `pwd`:/spksrc synocommunity/spksrc /bin/bash -c "exit 127"
#  - docker run -it -v `pwd`:/spksrc synocommunity/spksrc /bin/bash -c "cd /spksrc && make setup && cd spk/$SPK && make arch-$SYNOARCH"
